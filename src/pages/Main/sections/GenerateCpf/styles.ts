import styled from 'styled-components'
import { IconButton } from '~components'

export const IconButtonStyled = styled(IconButton)`
  padding: ${({ theme }) => theme.spacing(0, 1, 0, 0)} !important;
`
